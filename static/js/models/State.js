/**
 * @interface
 */
export default class State {
    /**
     * Returns a user-friendly string describing the state.
     * @return {string} String describing state
     */
    toString() { return ""; }
}
